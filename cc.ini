[crosscompute]
command_template = python distances.py
                        --target_folder {target_folder}
                        -O {origins_text_path}
                        -D {destinations_text_path}
                        -M {mode}

origins_text_path = origin.txt
destinations_text_path = dest.txt
mode = driving
